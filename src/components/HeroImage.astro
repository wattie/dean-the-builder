---
const { heroImage, heroImageAlt } = Astro.props as {
  heroImage: string;
  heroImageAlt?: string;
};
---

<img
  src={heroImage}
  alt={heroImageAlt ?? "Hero image"}
  class="hero-image object-cover h-75 lg:h-100"

/>
