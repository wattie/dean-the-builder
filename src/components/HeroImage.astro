---
const { heroImage, heroImageAlt } = Astro.props as {
  heroImage: string;
  heroImageAlt?: string;
};
---

<img
  src={heroImage}
  alt={heroImageAlt ?? "Hero image"}
  class="hero-image object-contain h-[300px] lg:h-[400px]"
/>
